-- public.student definition

-- Drop table

-- DROP TABLE public.student;

CREATE TABLE public.student (
	id int8 NOT NULL GENERATED ALWAYS AS IDENTITY,
	first_name varchar NOT NULL,
	last_name varchar NOT NULL,
	desc_data jsonb NOT NULL,
	student_id int8 NOT NULL,
	CONSTRAINT student_pk PRIMARY KEY (id)
);


-- public.student foreign keys

ALTER TABLE public.student ADD CONSTRAINT student_fk FOREIGN KEY (student_id) REFERENCES public.course(id) ON DELETE SET NULL ON UPDATE CASCADE;